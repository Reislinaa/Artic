# ARTIC · 网站设计规范（DESIGN.md）

> 用途：本文件是给任何 AI 阅读的设计系统规范。无论使用哪个模型，
> 读完全文即可按统一的视觉语言生成 / 评审 / 修改本项目的 UI。
> 对标：Linear（克制）、Stripe（商务质感）、typeless（亮色极简长滚动）
> **配色依据**：《万物本相 UI DESIGN REFERENCE》(EMI · 2026.09) —— 黑白主导 + 橙色点缀
> 方向：高端商务场景的 AI 语音成稿工具

---

## 0. 产品定位（决定文案与视觉的一切）

- **品牌名**：ARTIC
- **Slogan**：让说出口的话，就是能直接发出去的稿 ／ `Say what you mean, but better.`
- **定位**：AI 语音转文字 + 润色，面向**高端商务场景**的表达效率工具
- **两大核心功能**：① 语音转文字 + 润色 ② 语音转文字 + 翻译 + 润色
- **目标人群**：高表达密度 × AI Power User 的交集——律师、金融从业者、高端销售、
  品牌策划、项目经理、自媒体商务对接者
- **定价**：¥29.9/月 · ¥299/年；企业版专属词包按人数定价
- **核心主张**：**润色质量是分水岭**。识别准只是及格线，「写得好」才是付费理由。
- **禁忌**：不要写成"AI 输入法大全""AI 助手""聊天机器人""产品库/推广平台"。

## 1. 视觉主题与气质

- **黑白主导 + 橙色点缀（Black & White + Orange Signal）**：
  依据《万物本相 UI DESIGN REFERENCE》(EMI · 2026.09 · 第 07 节)——
  黑与白是基础品牌色，在界面中**大面积主导**；黑白之外的第三种颜色
  **一律小面积、点缀式使用**，只作为视觉记忆点出现。
- **点缀色 = 橙**：色相**偏黄、明度偏高**，避免偏红（太橘）或偏暗；
  以**渐变 / 光晕**形式呈现；单个界面占比控制在 **5%~10% 以内**。
  只允许用在：① 关键行动按钮（CTA）② 核心数据高亮 ③ 品牌记忆点。
- **深色底 + 橙色光晕**是最具识别度的组合：大面积黑底、白色文字、
  橙色以柔和大光斑（`blur` 径向渐变）小面积出现。
- **克制是最高优先级**：元素越少越好，大留白，一屏一个视觉焦点；
  层级靠**留白与字号**，而不是分割线堆叠。
- **记忆点**：品牌符号为**官方 logo**（`public/artic-logo.png`，已去底透明），
  导航栏 / 页脚 / 站点图标统一使用，不要另画替代图形。
- **"AI 味"禁止清单**：多色霓虹渐变铺满、毛玻璃滥用、卡片套卡片、
  万能圆角+大阴影、发光椭圆、emoji 当图标、无意义装饰动画。

## 2. 色彩系统

> **唯一令牌来源是 `src/index.css`**。禁止在组件 CSS 里重复定义 `:root` 变量——
> 历史上 `Hero.css` 局部重定义过 `--bg/--primary/--text`，因 CSS 全局作用域 +
> 加载顺序**静默覆盖**了全局令牌，造成「改了 index.css 却看不到效果」。

| Token | 值 | 用途 |
|---|---|---|
| `--ink` | `#0A0A0A` | **墨**：深色区块底 / 浅色区主文字 |
| `--ink-2` | `#141414` | 次级墨（弹层、悬浮菜单） |
| `--paper` | `#FFFFFF` | 白：卡片表面 |
| `--paper-2` | `#F7F5F2` | **暖米白**：页面默认底色（不用冷白） |
| `--paper-3` | `#EFEBE5` | 轨道 / 弱背景 |
| `--on-ink` | `#FFFFFF` | 黑底上的主文字 |
| `--on-ink-2` / `--on-ink-3` | `rgba(255,255,255,.68)` / `.44` | 黑底上的次级 / 弱文字 |
| `--hairline-ink` | `rgba(255,255,255,.14)` | 黑底上的极细分割线 |
| `--accent` | `#FF8A1F` | **点缀橙**：光晕、深色底上的强调 |
| `--accent-bright` | `#FFA53D` | 点缀橙 · 亮态（hover） |
| `--accent-soft` | `#FFC24B` | 点缀橙 · 暖端（渐变起点） |
| `--accent-deep` | `#C2410C` | 浅色底上的橙色文字（保证对比度 ≥ 4.5:1） |
| `--grad-accent` | `linear-gradient(135deg,#FFC24B,#FF7A18)` | 橙色渐变（记忆点 / 头像 / 进度条） |

**规则**：
- **先问「这块能不能做成黑白」**，只有关键动作与关键数据才配用橙。
- 橙色**禁止大面积铺色**；浅色底上用橙写文字必须用 `--accent-deep`。
- 橙底按钮配**黑字**（对比度约 8:1），**不配白字**。
- 装饰性橙色光晕用**径向渐变 + 大模糊**（`filter: blur(46~70px)`）实现，不要用实心圆。
- 支付页可例外使用微信绿 `#07C160` / 支付宝蓝 `#1677FF`，但**仅限渠道图标**，
  属于功能色而非装饰色。

## 3. 排版系统

- 字体：`MiSans`（品牌字体，与商业计划书一致）→ 回退 `Noto Sans SC`。
- **字级对比要拉开**：大标题用**负字距**（`letter-spacing: -0.03em ~ -0.045em`），
  Hero 标题 `clamp(3rem, 9vw, 7rem)`；小字注释用 **11–13px + 大写 + 宽字距**（`letter-spacing: 3px+`）。
- **字重克制**：正文 400，强调 500/600，最高 700（禁止 800 以上粗体）。
- 正文行高放宽（1.6~1.85），行宽限制在 65~75 字符。
- **细线分割建立节奏**：区块标题下方统一用 `1px` 细线（`.section-subtitle::after`）；
  黑底区块用 `--hairline-ink`——靠**明度差与留白**区分层次，而不是靠色块。
- **「橙色序号」是标志性手法**：步骤 / 功能条用橙色大字编号（`01` `02` …），
  对应 PDF 里的标题序号版式（见 `StepsSection`、`FeatureShowcase` 的 `.feature-label`）。

## 4. 组件规范

- **按钮**：**主 CTA = 橙底黑字**（`--accent` + `--ink`，`box-shadow: var(--shadow-accent)`）；
  黑底区块上的次要按钮用 `.btn-on-ink`（白色描边）；浅色区次要按钮用 `.btn-ghost`（细边框）；
  圆角 999px（胶囊形）；hover 轻微上浮 `translateY(-2px)`，不做霓虹发光。
- **深色区块**：给 `<section>` 加 `.ink` 即获得黑底 + 反色文字 + 细线；
  装饰用 `.ink-glow`（柔和大光斑）或 `.ink-horizon`（底部中央橙色「地平线」光带），
  两者都必须 `pointer-events: none`。
- **产品模拟卡**（`InputMockup`）：白卡 + **墨黑气泡**，橙色只保留在**麦克风按钮**这一个
  关键动作上——它是全站橙色占比的标尺，改动前先对照 5%~10% 的规范。
- **卡片**：白底、`1px` 细边框、圆角 16~20px、轻阴影；
  hover 上浮 4~6px；重点卡片顶部加一条品牌渐变细线（3px）表示点亮，
  配一枚"最受欢迎"胶囊徽标。
- **区块**：交替 `--bg` / `--bg-2`；区块标题前加英文短语小标
  （uppercase，品牌色，字距 2~3px）。
- **图标**：线稿 SVG（stroke 1.5~2，单色，24×24）；品牌标用 `simple-icons` 官方 path；
  **禁止 emoji 当图标**。
- **品牌图标数据**（`src/data/app-icons.js`）必须由 `scripts/gen-icons.mjs` 生成：
  脚本对每个条目做**强校验**（取不到即报错退出），防止错误图标混入站点。
  注意 `simple-icons` **v16 已下架** Microsoft 全系 / Slack / OpenAI / Canva / Adobe /
  LinkedIn / 钉钉 / 飞书 / 抖音 / WPS 等，新增品牌前先确认该版本是否收录。
- **品牌图标来源必须可核验**：优先 `simple-icons` 官方 path；若该版本已下架
  （如飞书 / 钉钉），改用**官方渠道**取得的图标并**自托管**（`public/brand/`，数据字段用 `img`），
  **下载后必须目视核验**；不得凭印象手绘、也不得使用来源不明的图。
- **图标环的选品**：只收大众高频使用的**办公 / 社交**类 App（海内外兼顾），
  不收游戏、不收纯娱乐（音乐/流媒体）、不收小众开发者工具——否则会出现用户不认识的图标。
- **规格类内容用规格表，不要用卡片墙**：平台支持、参数对比这类信息用
  「图标+名称 | 状态标签 | 一句话能力」的行式表格（见 `PlatformSpecs.jsx`），
  一行一个条目，层级靠留白与字号；**卡片墙会把信息摊薄、并把同一信息重复展示两遍**。
- **「全场景通用」圆环**（`EverywhereSection`）：国内 + 国际常用软件交替混排，
  双层反向旋转（外环 40s / 内环 50s），图标卡片 62px、内环 53px。

## 5. 布局原则

- 容器最大宽度 1180px（宽版 1320px），两侧 28px 内边距。
- 间距用 **8pt 标尺**（8/16/24/32/48/56/72/120）。
- 区块垂直留白 120px，移动端 72px。
- 每屏只有一个视觉重心；Hero 用三段式（顶标/主内容/底提示）避免内容堆顶。
- **网格必须写 `minmax(0, 1fr)`，不要写 `1fr`**：网格子项默认 `min-width: auto`，
  内容的固有宽度会把整列撑宽、溢出容器（移动端表现为「页面没居中」）。
  同时给网格子项加 `min-width: 0`。已在 `.hero-content` 踩过一次。

## 6. 动效规范

- 用途优先级：**有含义的动效 > 装饰动效**。动效必须服务内容
  （滚动展示能力、语音波形收敛为文字行、速度对比表达卖点）。
- 缓动：入场用 `--ease-out: cubic-bezier(0.16, 1, 0.3, 1)`。
- 滚动渐入用 `.reveal` + `Reveal` 组件（IntersectionObserver），
  复杂滚动叙事用 GSAP ScrollTrigger。
- 微交互 150~300ms；入场动画 0.3~0.6s；stagger 0.04~0.08。
- 组件卸载必须清理监听器与 GSAP context。
- 尊重 `prefers-reduced-motion`。

## 7. 页面结构

| 页面 | key | 内容 |
|---|---|---|
| 首页 | `home` | Hero + Steps + FeatureShowcase + Everywhere + Platform |
| 功能页 | `features` | 六大商务场景 + CTA |
| **定价页** | `pricing` | 三档方案（月付 / 年付 / 企业版）+ FAQ |
| **收银台页** | `checkout` | 独立支付页：订单摘要 + 渠道切换 + 二维码 + 失效倒计时 + 轮询 + 成功态 |
| 下载页 | `download` | 平台选择 + FAQ |
| 关于页 | `about` | 信念 + 历程 + 联系 + CTA |
| 法律页 | `privacy` / `terms` | 隐私政策 / 服务条款 |

**路由**：极简 hash 路由（见 `App.jsx`），每个页面都有真实 URL，浏览器前进/后退可用。
支付流程：定价页点方案 → `#/checkout?plan=pro-yearly`（独立收银台）→ 支付成功。
收银台页面**隐藏页脚**以减少干扰，导航高亮保持在「定价」。

## 8. 支付链路架构（改支付相关代码前必读）

设计目标：**填入证件号即可上线，无需改代码**。

- `server/pay.js` 是唯一的支付实现层，两种模式自动切换：
  - **演示模式（demo）**：未配置商户参数。本地模拟下单 + 8 秒后自动置为已支付，
    用于展示与联调。`/api/order/forcepaid` 仅在演示模式可用。
  - **正式模式（live）**：`WECHAT_*` / `ALIPAY_*` 配齐后自动启用。
    微信走 APIv3 Native 下单（`code_url`），支付宝走当面付 `precreate`（`qrCode`），
    支付结果由官方异步回调写入订单状态。
- 前端通过 `GET /api/pay/status` 得知当前模式与缺失字段，并在页面上显示模式提示条。
- **金额以服务端价格表 `PLANS` 为准**，前端传方案 key，防止篡改。
- 正式模式下单失败**必须报错**，不得静默降级为演示（否则会出现"看似付款成功实则未进账"）。
- 微信回调需 `200 + {code:'SUCCESS'}`，支付宝回调需返回纯文本 `success`。
- 订单存储当前为内存实现；生产环境请落库并加唯一索引与幂等处理。

## 9. 账号与第三方登录架构（改登录相关代码前必读）

设计目标：**凭据填进 `.env` 即自动启用，无需改代码**。

- `server/oauth.js` 是唯一的第三方登录实现层。`PROVIDERS` 注册表已实现 4 个渠道：
  **微信**（开放平台「网站应用」扫码登录）/ **QQ 互联** / **飞书** / **钉钉**。
  `RESERVED_PROVIDERS` 为预留占位：企业微信 / 支付宝 / GitHub / Google / Apple。
- 每个渠道只需声明 `envKeys` + `authorizeUrl` + `exchange` + `user` 四件事；
  **新增渠道 = 在 `PROVIDERS` 里补一条**，前端无需改动。
- `GET /api/auth/providers` 返回各渠道 `configured` / `missing`，前端据此决定按钮可点或灰显。
- 前端兜底：`src/data/auth-providers.js` 提供渠道列表，
  **无后端（纯静态部署）时仍渲染出预留入口**，保证 UI 结构稳定。
- 会话机制：HMAC 签名 token（`SESSION_SECRET`），前端存 `localStorage`，
  请求带 `Authorization: Bearer`；**不依赖 Cookie**，便于前后端分离 / 跨域部署。
- 安全要点：
  - `state` 必须签名校验（防 CSRF）
  - 回跳 token 经 URL 参数传递，前端读取后**立即 `replaceState` 清除**
  - `safeUser()` 必须剥离密码等敏感字段，任何接口都不得返回 `password`
  - 第三方登录用户 `password` 为 `null`，禁止走密码登录
  - **密码一律 scrypt 哈希**（`server/password.js`，格式 `scrypt$N$r$p$salt$hash`），
    绝不落明文；校验返回 `needsRehash` 以兼容历史明文数据并在登录成功后透明迁移
- **品牌图标注意**：飞书 / 钉钉官方图标在 `simple-icons` v16 已下架，
  当前用品牌色首字占位；拿到官方 SVG 后替换 `AuthModal.jsx` 里的 `ICON_MAP` 即可。

## 10. 大厂对标工作流（给 AI 的自检流程）

生成或修改 UI 前，先问自己：
1. 这个页面一屏的**视觉焦点**是什么？其他元素是否在让路？
2. 我用了几个强调色？**超过 1 个就该收敛**（支付渠道图标除外）。
3. 每个动画是否**讲了一件事**？删掉后页面是否更干净？
4. 内容是否服务于"高端商务表达效率"这一件事？

**评审清单（改稿前过一遍）**：
- [ ] 无多色渐变铺满、无发光椭圆、无 emoji 图标
- [ ] 标题负字距、字重 ≤700、大留白
- [ ] 强调色仅出现在 CTA/选中态/关键数字
- [ ] 动效有含义、有清理、尊重 reduced-motion
- [ ] 文案与"语音成稿工具"强相关，无输入法大全/推广平台残留
- [ ] 定价文案与 `PLANS`（¥29.9/月、¥299/年）一致

## 11. 反模式（Don't）

- ❌ 多色霓虹渐变铺满、深色背景+发光文字
- ❌ 纯黑 `#000` / 纯白 `#fff` 大面积使用
- ❌ emoji、通用圆角卡片套卡片、毛玻璃滥用
- ❌ 无意义的粒子/漂浮/旋转装饰
- ❌ 为炫而炫的动画（弹性、扭曲、大幅位移）
- ❌ 在公开页面泄露商业计划书内部资料（财务预测、股东履历、竞品实测细节）
- ❌ 用大卡片墙承载「平台支持 / 规格参数」这类内容（信息被摊薄，且容易把同一信息展示两遍）
- ❌ 凭印象手绘品牌 logo，或使用来源不明、无法核验的图标
- ❌ 在图标墙里塞小众工具 / 游戏 / 娱乐类 App（用户不认识 = 纯噪声）
