# 依赖
node_modules/

# 构建产物
dist/

# 部署临时目录
.gh-pages-tmp*/

# 日志与部署辅助
deploy-log.txt

# 环境变量（可能含密钥）
.env
.env.*
!.env.example
*.local

# 运行时数据（用户账号、日志等，绝不入库）
server/data/

# 临时分析/调试文件（本项目约定：临时脚本与产物一律以 _ 开头）
# 已多次误提交过 _extract.py / _pdf_*.png 等，此规则用于防复发
_*


# 系统文件
.DS_Store
Thumbs.db
